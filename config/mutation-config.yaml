# ---------------------------------------------------------
# CHANGE THIS SECTION TO SWITCH TARGETS
# ---------------------------------------------------------

OPTION 1: HTTP API (Keep commented out when not using)
target_system:
  type: "api"
  url: "http://localhost:8080/api/v1/test"
  method: "POST"
  timeout_seconds: 5

# OPTION 2: Local Python Function (Rule Engine)
# target_system:
#  type: "function"
#  module_path: "local_targets"     # File name (local_targets.py)
#  function_name: "process_order_rules" # Function inside the file
#  timeout_seconds: 1

# # OPTION 3: AI Model (Uncomment to test AI)
# target_system:
#   type: "function"
#   module_path: "local_targets"
#   function_name: "ai_inference_wrapper"
#   timeout_seconds: 3

test_settings:
  concurrency: 20 # Lower concurrency for local CPU bound tasks
  total_requests: 1000

mutations:
  - type: "long_string"
  - type: "boundary_int"
  - type: "unicode"
  - type: "nested_json"